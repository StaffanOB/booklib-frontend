# Environment variables for BookLib Frontend
# Copy this file to .env and customize as needed

# Frontend configuration
NODE_ENV=production
REACT_APP_API_URL=http://booklib-api:5000

# Service Communication Notes:
# - REACT_APP_API_URL uses container name 'booklib-api' for Docker network communication
# - This assumes booklib-api service is deployed and running on the same Docker network
# - External access: Frontend (3000), API (5000), Database (5432)
# 
# Independent Service Architecture:
# - booklib-db: PostgreSQL database service
# - booklib-api: Flask API backend service  
# - booklib-frontend: React frontend service (this repo)
# - All services communicate via 'booklib-net' Docker network